var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
import { logger } from '../../shared/logger.js';
import { initTaskRunner } from '../../shared/processConcurrency.js';
/**
 * Execute globby in worker to isolate memory usage
 */
export const executeGlobbyInWorker = (patterns_1, options_1, ...args_1) => __awaiter(void 0, [patterns_1, options_1, ...args_1], void 0, function* (patterns, options, deps = {
    initTaskRunner,
}) {
    const taskRunner = deps.initTaskRunner({
        numOfTasks: 1,
        workerPath: new URL('./workers/globbyWorker.js', import.meta.url).href,
        runtime: 'child_process',
    });
    try {
        logger.trace('Starting globby in worker for memory isolation');
        const startTime = process.hrtime.bigint();
        const result = yield taskRunner.run({
            patterns,
            options,
        });
        const endTime = process.hrtime.bigint();
        const duration = Number(endTime - startTime) / 1e6;
        logger.trace(`Globby completed in worker in ${duration.toFixed(2)}ms`);
        return result;
    }
    catch (error) {
        logger.error('Error during globby execution:', error);
        throw error;
    }
    finally {
        // Always cleanup worker pool
        yield taskRunner.cleanup();
    }
});
//# sourceMappingURL=globbyExecute.js.map