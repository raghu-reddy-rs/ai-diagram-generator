version: '3.8'

services:
  diagram-generator:
    build: .
    container_name: diagram-generator
    env_file:
      - .env
    volumes:
      - ./output:/app/output
    tty: true
    stdin_open: true
